package main 

import (
    . "LibScan" //' project (aka system under test)
    //"fmt"
    //. "myUtils" //' file utils and test utils
)

// unit tests and std run for src/goLibrarian/GoLibrarian
func main() {
    //' un-comment one of the following:
    LibScan()

    //testGoLibrarian()
}

